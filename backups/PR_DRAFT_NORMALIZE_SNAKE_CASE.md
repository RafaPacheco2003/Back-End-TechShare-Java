Title: feat(normalize): normalize DB & JSON to snake_case; compatibility and tests

Summary
-------
This branch applies the "Option 1" strategy: keep Java code in camelCase while normalizing the database schema and JSON payloads to snake_case. It adds compatibility accessors, fixes fragile Spring Data derived queries, unifies image validation API, and ensures MoveType enum maps legacy tokens (OUT/IN).

Changes
-------
- Jackson configured to serialize/deserialize in snake_case.
- Added compatibility getters/setters in entities (e.g., getMaterialsId, getBorrowId, getNombre aliases).
- Replaced fragile derived repository methods with explicit @Query where needed (CategoriesRepository, UsuarioRoleRepository, MaterialsRepository). Added optimized repository methods (findBy*Optimized).
- Unified ImageValidationStrategy to accept MultipartFile; updated implementations and call sites. Resolved bean ambiguity with @Primary.
- MoveType enum: added @JsonCreator/@JsonValue to accept legacy tokens (OUT/IN) and emit them.
- Added unit and integration tests: MoveTypeTest, JacksonSnakeCaseTest, MoveTypeJacksonIntegrationTest.
- Added Flyway migration: V1__Initial_Schema_Normalized.sql (creates normalized snake_case schema). Note: this migration replaces previous schema and will DROP tables if executed on a DB.

Files changed (high level)
-------------------------
- src/main/java/... (entities): compatibility getters/setters and Jackson config
- src/main/java/.../repository: CategoriesRepository, MaterialsRepository, UsuarioRoleRepository, BorrowRepository (optimizations)
- src/main/java/.../validation: ImageValidationStrategy + implementations
- src/main/java/.../entity/MoveType.java
- src/main/resources/db/migration/V1__Initial_Schema_Normalized.sql
- src/test/java/...: unit & integration tests

Checklist for reviewers
----------------------
- [ ] Run full test suite: `./mvnw test` (already passing locally: 77 tests, 0 failures)
- [ ] Review Flyway migration carefully — V1 drops existing tables and recreates schema. DO NOT apply directly to production. Prefer running on a staging copy first and ensure data migration plan.
- [ ] Review changes to repositories and services for any naming/compatibility issues with external clients.
- [ ] Frontend: ensure clients expect snake_case JSON (or add a client-side adapter).

How to validate locally
-----------------------
1. Run tests: `./mvnw test`
2. Start app for manual smoke tests (dev): `./mvnw spring-boot:run` (ensure DB is configured or use test profile)
3. Check Flyway migrations in your staging DB:
   - `SELECT * FROM flyway_schema_history ORDER BY installed_rank DESC;`
   - or run `./mvnw org.flywaydb:flyway-maven-plugin:info -Dflyway.url=jdbc:<your-db-url> -Dflyway.user=<user> -Dflyway.password=<pass> -Dflyway.locations=filesystem:src/main/resources/db/migration`

Notes & Warnings
----------------
- The migration `V1__Initial_Schema_Normalized.sql` is destructive: it drops multiple tables. Do not run it against production without a careful migration & data-mapping strategy.
- If the frontend expects camelCase, you'll need to update it to consume snake_case or add a compatibility layer.

Next steps (I can do)
---------------------
1. Open a PR draft text on GitHub (I can prepare the body — you must push and create the PR). 
2. Run smoke tests against a staging DB (I can attempt locally if you provide a staging DB URL or run commands yourself and paste results).
3. If you want, I can add endpoint-level integration tests for critical flows (login, create movement).

----
Generated by automation on 2025-10-24
